fastapi
uvicorn
openai
python-dotenv
pdfplumber
openpyxl
python-docx
tqdm
pydantic
pillow
python-multipart
supabase==2.28.3
# Optional: Laya System-1 decision model for the email-triage neural tier.
# NOT for Render free tier — pulls torch+transformers (~3GB). Installed by the
# Modal image in modal_app.py for the live deployment; local/demo install:
#   pip install laya==0.3.4
# laya==0.3.4
